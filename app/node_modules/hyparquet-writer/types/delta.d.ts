/**
 * Write values using delta binary packed encoding.
 *
 * @param {Writer} writer
 * @param {DecodedArray} values
 */
export function deltaBinaryPack(writer: Writer, values: DecodedArray): void;
/**
 * Write byte arrays using delta length encoding.
 * Encodes lengths using delta binary packed, then writes raw bytes.
 *
 * @param {Writer} writer
 * @param {DecodedArray} values
 */
export function deltaLengthByteArray(writer: Writer, values: DecodedArray): void;
/**
 * Write byte arrays using delta encoding with prefix compression.
 * Stores common prefixes with previous value to improve compression.
 *
 * @param {Writer} writer
 * @param {DecodedArray} values
 */
export function deltaByteArray(writer: Writer, values: DecodedArray): void;
import type { Writer } from '../src/types.js';
import type { DecodedArray } from 'hyparquet';
//# sourceMappingURL=delta.d.ts.map